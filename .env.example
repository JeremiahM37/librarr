# =============================================================================
# Librarr — Environment Configuration
# Copy to .env and fill in your values.
# Optional integrations are commented out — uncomment to enable.
# =============================================================================

# ── Storage paths (adjust to your setup) ─────────────────────────────────────
# Where downloaded ebooks are staged before import
INCOMING_DIR=/data/books/incoming

# Where organized ebooks are stored (Author/Title/ structure)
EBOOK_ORGANIZED_DIR=/data/books/ebooks

# Where organized audiobooks are stored
AUDIOBOOK_DIR=/data/books/audiobooks

# ── Prowlarr (optional — enables torrent search) ──────────────────────────────
# PROWLARR_URL=http://prowlarr:9696
# PROWLARR_API_KEY=your_prowlarr_api_key

# ── qBittorrent (optional — enables torrent downloads) ────────────────────────
# QB_URL=http://qbittorrent:8080
# QB_USER=admin
# QB_PASS=your_qbittorrent_password
# QB_SAVE_PATH=/books-incoming/
# QB_CATEGORY=books
# QB_AUDIOBOOK_SAVE_PATH=/audiobooks-incoming/
# QB_AUDIOBOOK_CATEGORY=audiobooks

# ── Audiobookshelf (optional — enables audiobook library + metadata matching) ─
# ABS_URL=http://audiobookshelf:80
# ABS_TOKEN=your_abs_api_token
# ABS_LIBRARY_ID=your_audiobook_library_id
# ABS_EBOOK_LIBRARY_ID=your_ebook_library_id
# ABS_PUBLIC_URL=http://your.server:13378

# ── Calibre-Web (optional — auto-import via calibredb) ────────────────────────
# CALIBRE_CONTAINER=calibre-web
# CALIBRE_LIBRARY=/data/books/ebooks
# CALIBRE_LIBRARY_CONTAINER=/books

# ── Kavita (optional — ebook library scan) ───────────────────────────────────
# KAVITA_URL=http://kavita:5000
# KAVITA_API_KEY=your_kavita_opds_api_key
# KAVITA_LIBRARY_ID=1

# ── lightnovel-crawler (optional — web novel EPUB scraping) ──────────────────
# LNCRAWL_CONTAINER=lncrawl

# ── Import targets (comma-separated: calibre, audiobookshelf, kavita) ─────────
# ENABLED_TARGETS=calibre,audiobookshelf

# ── File organization ─────────────────────────────────────────────────────────
# FILE_ORG_ENABLED=true

# ── Internal settings ─────────────────────────────────────────────────────────
# LIBRARR_DB_PATH=/data/librarr/downloads.db
# LIBRARR_SETTINGS_FILE=/data/librarr/settings.json
